.env
.ipynb_checkpoints/
__pycache__/
*.pyc
Dataset/
dataset/
runs/
*.pt
!best_yolov8n.pt
!best_yolov8s.pt

# --- Voice agent app ---
.venv/
uploads/
*.db
.pytest_cache/

# runtime-served duplicate of the raw clip, which is tracked at the repo root
static/test_clip.mp4

# tracking outputs
*_tracked.mp4
*_tracks.csv
*_tracks.json

# ...except the dashboard's demo clip, which ships with the repo so the landing page
# works on a fresh clone without anyone re-running track_video.py
!static/test_clip_tracked.mp4

# synthetic belt clips
belt_*.mp4
belt_*_gt.json
